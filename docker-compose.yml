version: '2'
services:
  db:
    image: postgres
    ports:
      - "5432:5432"
  server:
    build: .
    command: ["puma"]
    volumes:
      - .:/palladium
    ports:
      - "80:80"
    depends_on:
      - db
