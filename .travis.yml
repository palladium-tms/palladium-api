sudo: required

language: ruby

services:
         - docker

before_install:
              - docker build . -t palladium
              - docker run --name db -itd postgres
              - docker run --name server --link db:postgres -itd -p 80:80 palladium puma

install:
        - bundle install

script:
      - bundle exec rspec spec/tests/
