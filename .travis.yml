sudo: required

language: ruby

services:
         - docker

before_install:
              - docker build . -t palladium
              - docker run -d -p 3000:3000 palladium puma

install:
        - bundle install

script:
      - bundle exec rspec spec/tests/
